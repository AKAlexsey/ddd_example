<%= render KalturaAdmin.LayoutView, "entity_header.html",
      entity_name: :subnet,
      mode:        :show,
      item:        @subnet
%>

<%= render KalturaAdmin.LayoutView, "table_form.html",
      link_cancel:  region_path(@conn, :show, @region),
      link_delete:  subnet_path(@conn, :delete, @subnet),
      link_edit:    subnet_path(@conn, :edit,   @subnet),
      mode:         :show,
      item:         @subnet,
      meta:         KalturaAdmin.RegionView.subnet_meta()
 %>
