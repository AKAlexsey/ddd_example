<%= render KalturaAdmin.LayoutView, "entity_header.html",
      entity_name: "subnet for Region##{@region.id}",
      mode:        :create
%>

<%= render KalturaAdmin.LayoutView, "form_view.html",
      action:       subnet_path(@conn, :create),
      link_cancel:  region_path(@conn, :show, @region),
      changeset:    @changeset,
      mode:         :create,
      meta:         KalturaAdmin.RegionView.subnet_meta()
 %>
