<h2>Listing users</h2>

<table class="table">
  <thead>
    <tr>
      <th>Email</th>
      <th>First name</th>
      <th>Last name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for user <- @users do %>
    <tr>
      <td><%= user.email %></td>
      <td><%= user.first_name %></td>
      <td><%= user.last_name %></td>

      <td class="text-right">
        <span><span><%= link "Show", to: user_path(@conn, :show, user), class: "btn btn-default btn-xs", class: "btn btn-default btn-xs" %></span></span>
        <span><span><%= link "Edit", to: user_path(@conn, :edit, user), class: "btn btn-default btn-xs", class: "btn btn-default btn-xs" %></span></span>
        <span><span><%= link "Delete", to: user_path(@conn, :delete, user), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs", class: "btn btn-danger btn-xs" %></span></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New user", to: user_path(@conn, :new) %></span>
